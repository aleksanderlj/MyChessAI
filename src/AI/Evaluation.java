package AI;

import logic.Board;
import logic.pieces.*;

public class Evaluation {

    public void minimax(Board board, int depth, boolean maxPlayer){
        if(depth == 0 ) { // || TODO Game is over

        }

        // WHITE
        if(!maxPlayer){
            int maxEval = Integer.MIN_VALUE;

        }

    }
}
